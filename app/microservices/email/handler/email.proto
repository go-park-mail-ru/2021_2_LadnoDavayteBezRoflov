syntax = "proto3";

package handler;

option go_package = "./handler";

message UserInfo {
  string login = 1;
  string email = 2;
}

message Nothing {
  bool dummy = 1;
}

// grpc-сервис проверки авторизации
service EmailSender {
  rpc SendEmail (UserInfo) returns (Nothing) {}
}